# 🤖 Freelance Parser Bot

Telegram бот для автоматического парсинга заказов с фриланс-площадок и уведомления о новых проектах.

## 📋 Особенности

- **Автоматический парсинг** заказов каждые 5 минут
- **Telegram уведомления** о новых заказах с кнопками для перехода
- **Поддержка FreelanceSpace.ru** с возможностью расширения
- **Две версии**: полная с БД и простая без БД
- **Извлечение ссылок** на оригинальные заказы
- **Предотвращение спама** - уведомления только о новых заказах

## 🚀 Версии

### 📊 Полная версия (с базой данных)
- **Файлы**: `telegram_bot.py`, `start_bot.py`, `run_bot.bat`
- **БД**: SQLite для хранения заказов
- **Особенности**: Полная история, статистика, фильтрация по времени

### 🎯 Простая версия (без базы данных)  
- **Файлы**: `telegram_bot_simple.py`, `start_bot_simple.py`, `run_bot_simple.bat`
- **Хранение**: Только в памяти
- **Особенности**: Легкость, быстрота, самый новый заказ отправляется последним

## ⚙️ Установка и настройка

### 1. Клонирование и установка зависимостей

```bash
git clone <repository-url>
cd parser
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Создайте файл `config.py`:

```python
# Настройки Telegram бота
TELEGRAM_BOT_TOKEN = "your_bot_token_here"
CHAT_ID = "your_chat_id_here"

# Настройки парсера
USER_AGENT = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/136.0.0.0 Safari/537.36"
COOKIES = {}

# Интервал парсинга (в секундах)
PARSE_INTERVAL = 300  # 5 минут
```

### 3. Получение токена бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте полученный токен в `config.py`

### 4. Получение Chat ID

1. Напишите боту любое сообщение
2. Запустите `python check_chat_id.py` (если есть)
3. Или используйте @userinfobot для получения вашего Chat ID

## 🎮 Запуск

### Простая версия (рекомендуется)
```bash
# Linux/Mac
python start_bot_simple.py

# Windows
run_bot_simple.bat
```

### Полная версия (с БД)
```bash
# Linux/Mac  
python start_bot.py

# Windows
run_bot.bat
```

## 📱 Команды бота

- `/start` - Главное меню с кнопками управления
- `/help` - Справка по использованию  
- `/status` - Текущий статус работы парсера
- `/parse` - Запустить парсинг вручную
- `/toggle` - Включить/выключить автопарсинг

## 🔧 Утилиты

### Тестирование
```bash
# Простая версия
python test_parser_simple.py

# Полная версия  
python test_parser.py
python test_recent.py
```

### Управление БД (только полная версия)
```bash
python view_orders.py      # Просмотр заказов
python migrate_db.py       # Миграция БД
python check_db.py         # Проверка БД
```

## 📁 Структура проекта

```
parser/
├── parsers/
│   ├── freelancespace_parser.py         # Полный парсер с БД
│   └── freelancespace_parser_simple.py  # Простой парсер без БД
├── telegram_bot.py                      # Полный бот с БД
├── telegram_bot_simple.py               # Простой бот без БД
├── start_bot.py                         # Запуск полного бота
├── start_bot_simple.py                  # Запуск простого бота
├── run_bot.bat                          # Windows батник (полная версия)
├── run_bot_simple.bat                   # Windows батник (простая версия)
├── config.py                            # Конфигурация
├── requirements.txt                     # Зависимости Python
└── README.md                            # Документация
```

## 🌐 Поддерживаемые площадки

- ✅ **FreelanceSpace.ru** - полная поддержка с извлечением ссылок
- 🚧 **Другие площадки** - планируется добавление

## 🎯 Логика работы

### Простая версия
1. Парсит заказы с FreelanceSpace.ru каждые 5 минут
2. Сравнивает с отправленными заказами в памяти  
3. Отправляет уведомления только о новых заказах
4. **Порядок**: самый новый заказ отправляется последним
5. При перезапуске может быть дублирование

### Полная версия  
1. Сохраняет все заказы в SQLite базу данных
2. Отправляет уведомления только о заказах из последних 10 минут
3. Ведёт полную статистику и историю
4. Защита от дублирования при перезапуске

## 🛠️ Технические детали

### Зависимости
- `python-telegram-bot` - Telegram API
- `requests` - HTTP запросы  
- `beautifulsoup4` - Парсинг HTML
- `sqlite3` - База данных (встроен в Python)

### Особенности парсинга
- Использует POST запросы к API FreelanceSpace.ru
- Автоматическое извлечение ссылок на заказы
- Обработка различных форматов цен и описаний
- Устойчивость к изменениям структуры сайта

## 🚨 Решение проблем

### Ошибка "lxml not found"
Простые версии используют встроенный `html.parser` вместо `lxml` - проблема решена.

### Бот не отвечает
1. Проверьте токен бота в `config.py`
2. Убедитесь, что бот добавлен в чат
3. Проверьте Chat ID

### Нет новых заказов
1. Проверьте интернет-соединение
2. Возможно, новых заказов действительно нет
3. Попробуйте ручной парсинг командой `/parse`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены  
3. Проверьте конфигурацию в `config.py`

## 📈 Планы развития

- 🔄 Добавление новых фриланс-площадок
- 🎛️ Веб-интерфейс для управления
- 📊 Расширенная аналитика
- 🔍 Фильтры по категориям и ценам
- 📱 Поддержка нескольких чатов

---

**Рекомендация**: Для большинства пользователей подойдёт простая версия без базы данных. Она быстрее, проще и выполняет основную задачу - уведомления о новых заказах в правильном порядке (самый новый последний). 