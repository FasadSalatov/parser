# Настройки производительности для Freelance Parser Bot

# ====== СКОРОСТЬ ПАРСИНГА ======

# Количество страниц для парсинга (чем меньше - тем быстрее)
# 1 страница = ~12 заказов, время ~2-3 секунды
# 3 страницы = ~36 заказов, время ~6-8 секунд  
# 5 страниц = ~60 заказов, время ~10-12 секунд
MAX_PAGES_FAST = 5      # Для быстрого парсинга (по умолчанию)
MAX_PAGES_FULL = 10      # Для полного парсинга

# ====== СКОРОСТЬ ОТПРАВКИ ======

# Параллельная отправка (True = максимальная скорость, False = последовательная)
PARALLEL_SENDING = True

# Количество одновременных отправок (если PARALLEL_SENDING = True)
MAX_CONCURRENT_SENDS = 10

# ====== НАСТРОЙКИ ЗАПРОСОВ ======

# Таймаут для HTTP запросов (секунды)
REQUEST_TIMEOUT = 30

# Задержка между страницами (секунды, 0 = без задержек)
PAGE_DELAY = 0.0

# ====== ЛОГИРОВАНИЕ ПРОИЗВОДИТЕЛЬНОСТИ ======

# Показывать детальную информацию о времени выполнения
DETAILED_TIMING = True

# Показывать скорость обработки (заказов/секунду)
SHOW_SPEED_METRICS = True

# ====== РЕЖИМЫ РАБОТЫ ======

class PerformanceMode:
    """Предустановленные режимы производительности"""
    
    # Максимальная скорость (меньше заказов, но очень быстро)
    ULTRA_FAST = {
        'max_pages': 1,
        'parallel_sending': True,
        'page_delay': 0.0,
        'description': 'Ультра-быстрый режим: 1 страница, ~12 заказов за 2-3 сек'
    }
    
    # Быстрый режим (баланс скорости и полноты)
    FAST = {
        'max_pages': 3,
        'parallel_sending': True, 
        'page_delay': 0.0,
        'description': 'Быстрый режим: 3 страницы, ~36 заказов за 6-8 сек'
    }
    
    # Полный режим (все заказы, медленнее)
    FULL = {
        'max_pages': 5,
        'parallel_sending': True,
        'page_delay': 0.0,
        'description': 'Полный режим: 5 страниц, ~60 заказов за 10-12 сек'
    }
    
    # Безопасный режим (с задержками, для нестабильных соединений)
    SAFE = {
        'max_pages': 3,
        'parallel_sending': False,
        'page_delay': 0.5,
        'description': 'Безопасный режим: с задержками, для медленных соединений'
    }

# Текущий режим (измените для изменения производительности)
CURRENT_MODE = PerformanceMode.FAST

def get_performance_settings():
    """Получить текущие настройки производительности"""
    return CURRENT_MODE

def print_performance_info():
    """Показать информацию о текущем режиме производительности"""
    mode = get_performance_settings()
    print(f"🚀 Режим производительности: {mode['description']}")
    print(f"📄 Страниц для парсинга: {mode['max_pages']}")
    print(f"⚡ Параллельная отправка: {'Да' if mode['parallel_sending'] else 'Нет'}")
    print(f"⏱️ Задержка между страницами: {mode['page_delay']} сек") 